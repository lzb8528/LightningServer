启动方式
推荐：Docker 一键部署（Linux/WSL）

cd C:\Users\86151\Desktop\LightningServer\docker
docker compose up -d --build
访问 http://localhost

本地前端开发

cd frontend
npm install
npm run dev
访问 http://localhost:5173

本地后端编译（需 Linux/WSL）


cd backend
mkdir build && cd build
cmake .. && make -j$(nproc)
./lightning_server